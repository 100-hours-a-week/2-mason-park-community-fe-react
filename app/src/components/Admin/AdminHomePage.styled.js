import styled from "styled-components";

const S = {
    Wrapper: styled.div`
        padding: 10px 0;
        
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        
    `
}

export default S;